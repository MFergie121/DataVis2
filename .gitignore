# OS
.DS_Store

# Node (validation harness deps — package.json + lockfile are tracked, not the install)
node_modules/

# Local tooling — not for public release
.gstack/
.claude/
.agents/
skills-lock.json

# Project / AI instruction files — kept local
CLAUDE.md
AGENTS.md

# Private source material — copyrighted course PDFs, source data, and the
# planning/design knowledge base. These live only on the author's machine and
# are deliberately NOT tracked in this public repository.
raw/
wiki/
